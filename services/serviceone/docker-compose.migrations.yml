version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=ServiceOne_Identity
      - ServiceOne_DB=ServiceOne_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
